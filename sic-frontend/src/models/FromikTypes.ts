export interface SearchInitValues {
    searchValue: string;
  }

  export interface LoginInitValues {
    email: string;
    password: string;
  }

  export interface RegisterInitValues {
    first_name: string;
    surname: string;
    password: string;
    email: string;
  }
